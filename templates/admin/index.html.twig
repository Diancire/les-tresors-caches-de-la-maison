{# {% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>User index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Email</th>
                <th>Roles</th>
                <th>Password</th>
                <th>Nickname</th>
                <th>First_name</th>
                <th>Last_name</th>
                <th>Create_at</th>
                <th>Avatar</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.id }}</td>
                <td>{{ user.email }}</td>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                <td>{{ user.password }}</td>
                <td>{{ user.nickname }}</td>
                <td>{{ user.firstName }}</td>
                <td>{{ user.lastName }}</td>
                <td>{{ user.createAt ? user.createAt|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ user.avatar }}</td>
                <td>
                    <a href="{{ path('app_admin_show', {'id': user.id}) }}">show</a>
                    <a href="{{ path('app_admin_edit', {'id': user.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="10">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_new') }}">Create new</a>
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block title %}Dashboard | Mon profil{% endblock %}

{% block body %}
<div class="dashboard_container">
    <button id="scrollToTopBtn" class="btn-scroll-to-top">
        <i class="fas fa-arrow-up"></i>
    </button>
    {{ include('admin/_navbar_dashboard.html.twig') }}
    <div class="dashboard_container_content">
        <h1 class="dashboard_title">Mon profil</h1>
        <table class="dashboard_table">
            <tbody class="dashboard_table-tbody">
            {% for user in users %}
                <tr>
                    <th>Pseudo</th>
                    <td>{{ user.nickname }}</td>
                </tr>
                <tr>
                    <th>Roles</th>
                    <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
                </tr>
                <tr>
                    <th>Prénom</th>
                    <td>{{ user.firstName }}</td>
                </tr>
                <tr>
                    <th>Nom</th>
                    <td>{{ user.lastName }}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{{ user.email }}</td>
                </tr>
                <tr>
                    <th>Date d'enregistrement</th>
                    <td>{{ user.createAt ? user.createAt|date('Y-m-d H:i:s') : '' }}</td>
                </tr>
                <tr>
                    <th>Modifier/Supprimer</th>
                    <td class="dashboard_table-tbody-delete-edit">
                        {{ include('admin/_delete_form_users.html.twig') }}/
                        <a href="{{ path('app_admin_edit_user', {'id': user.id}) }}" class=""><i class="fa-solid fa-pen-to-square"></i></a>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="10">no records found</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
        {# <div class="dashboard_btn_container">
            <a href="{{ path('app_admin_new') }}">Create new</a>
            <a href="{{ path('app_admin_edit', {'id': user.id}) }}" class="btn btn_filled_green">Modifier</a>
            <a href="{{ path('app_admin_index') }}" class="btn btn_outlined_golden"><i class="fa-solid fa-arrow-left-long"></i>  Retour à la page précédente</a>
        </div> #}
    </div>
</div>
{% endblock %}

